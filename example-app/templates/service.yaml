apiVersion: v1
kind: Service
metadata:
  name: example-app-svc
  labels:
    app: example-app
spec:
  type: LoadBalancer 
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: example-app